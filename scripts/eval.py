from utils.config import load_config
from utils.logging import get_logger

log = get_logger("eval")

def main() -> None:
    cfg = load_config()
    log.info("Evaluation script stub. Will be completed in Phase 4 & 5.")

if __name__ == "__main__":
    main()
